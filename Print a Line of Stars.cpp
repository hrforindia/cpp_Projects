#include <iostream>
using namespace std;

int main() {
    int numStars = 10; // Number of stars to print

    // Print stars in a line
    for (int i = 0; i < numStars; ++i) {
        cout << "* ";
    }
    cout << endl;

    return 0;
}